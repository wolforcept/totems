package classes.objects.enemies;


public class EnemyWeeko {

	// private final static double HEALTH_MULTIPLIER = 0.6;
	// private final static int EXP_REWARD = 2;
	// private Point tar;
	//
	// public EnemyWeeko(Data data, Path path) {
	// super(data, data.getCurrentEnemyHealth() * HEALTH_MULTIPLIER, path,
	// data.getCurrentReward(), EXP_REWARD);
	// setImages("enemies/weeko");
	// tar = path.nextPoint();
	// addResistance(Elemento.FIRE, 1);
	// setSpeed(1);
	// if(Math.random()<0.5){
	// setInitialShield(data.getCurrentEnemyHealth() * HEALTH_MULTIPLIER);
	// }
	// }
	//
	// @Override
	// public void moveToNextPointInPath(double speed) {
	//
	// }

}
