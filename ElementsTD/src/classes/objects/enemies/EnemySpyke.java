package classes.objects.enemies;

import classes.main.Data;

public class EnemySpyke {

	// private final static double HEALTH_MULTIPLIER = 0.8;
	// private final static int EXP_REWARD = 2;
	// private Point tar;

	public EnemySpyke(Data data, Path path) {
		// addResistance(Elemento.WATER, 1.0);
		//
		// setSpeed(0.8);
	}

	public void moveToNextPointInPath(double speed) {

		// if (tar == null) {
		// endPath();
		// } else {
		// if (Auxi.point_distance(getX(), getY(), tar.x, tar.y) <= speed) {
		//
		// setX(tar.x);
		// setY(tar.y);
		// tar = getPath().nextPoint();
		//
		// } else {
		//
		// double dir = Auxi.point_direction(getX(), getY(), tar.x, tar.y);
		//
		// addX(speed * Math.cos(dir));
		//
		// addY(-speed * Math.sin(dir));
		//
		// }
		// }
	}

}
